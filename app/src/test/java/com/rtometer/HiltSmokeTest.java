package com.rtometer;

import org.junit.Test;

/**
 * Placeholder smoke test — Hilt injection is validated at build time by the annotation processor.
 */
public class HiltSmokeTest {

    @Test
    public void placeholder_passes() {
        // Hilt graph is verified by annotation processor during compilation.
    }
}
